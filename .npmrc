# The source shell (rokctai_frontend) is a pnpm workspace, which does not
# enforce peer ranges; react-day-picker@8 declares date-fns ^2||^3 while the
# tree rides date-fns 4. npm needs the same leniency for a plain `npm install`
# (what the SDK composer runs) to succeed.
legacy-peer-deps=true
