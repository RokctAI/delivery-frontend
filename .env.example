# Frappe / PaaS Configuration (bare shell)
ROKCT_BASE_URL=http://paas.localhost:8000
ROKCT_API_KEY=****
ROKCT_SECRET=****

# --- Required only once auth_sdk (users repo, auth/nextjs) is composed ---
# NextAuth secret. Generate a random secret: `openssl rand -base64 32`
AUTH_SECRET=****
# Local persistence (user/site + global settings tables, drizzle/Postgres —
# installed by auth_sdk)
POSTGRES_URL=****
